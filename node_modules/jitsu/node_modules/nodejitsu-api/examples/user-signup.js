var nj = require('../lib/client'),
    fs = require('fs');

var client = nj.createClient({
  remoteUri: 'http://api.nodejitsu.com'
});

client.users.create({
  username: "maraktest55556",
  email: "marak.squires@gmail.com"
  
}, function(err, result){
  if (err) {
    console.log(err);
    return;
  }
  console.log('result');
  console.log(result);
});
